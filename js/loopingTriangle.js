// Initialize an empty string
let triangleBuilt = "";

// Loop to generate the pattern
for (let i = 1; i <= 7; i++) {
    triangleBuilt += "#";  // Add a "#" each iteration
    console.log(triangleBuilt); // Print the current line
}
